import { ShowDescriptionsPipe } from './show-descriptions.pipe';

describe('ShowDescriptionsPipe', () => {
  it('create an instance', () => {
    const pipe = new ShowDescriptionsPipe();
    expect(pipe).toBeTruthy();
  });
});
